{
  "name": "crystal",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^4.1.2",
    "react-scripts": "3.1.1"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0"
  },
  "scripts": {
    "server": "concurrently \"npm start\" \"json-server --watch db.json --port 8080\"",
    "client": "node server.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
